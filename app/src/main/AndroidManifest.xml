<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.contactsnote">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Model.myApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.ContactsNote"
        tools:targetApi="31">
        <activity
            android:name=".ui.Activity_Profile"
            android:exported="false" />
        <activity
            android:name=".ui.ChooseActivityQuiz"
            android:exported="false" />

        <activity
            android:name=".ui.Queiz_Activity"
            android:exported="false" />
        <activity
            android:name=".ui.InfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.BookListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.choose_activity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>